# Tool-latent + eval + data pipeline (host python).
# Isaac Sim / Isaac Lab are installed separately (Windows-native).
torch
trimesh
scikit-learn
numpy
iisignature
vendi-score
lerobot
huggingface_hub
# RL (inside the Isaac Lab python env):
#   rsl-rl-lib
